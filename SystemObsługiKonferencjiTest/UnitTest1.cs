using System;
using System_obsługi_konferencji;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace SystemObsługiKonferencjiTesty
{
    [TestClass]
    public class Tests
    {
        [TestMethod]
        public void TestCompareToDane() //test sprawdzajacy metode CompareToDane (porownuje organizatorow po imieniu i nazwisku)
        {
            //Organizator o1 = new Organizator("Jan", "Nowak");
            //Organizator o2 = new Organizator("Adam", "Kowalski");
            //Assert.AreEqual(1, o1.CompareToDane(o2)); // jesli sa rozne, test bedzie pozytywny, jesli dane sa identyczne test zakonczy sie niepowodzeniem
        }
    }
}
